project(bind_qabstractbutton)

file(GLOB BIND_QABSTRACTBUTTON_SOURCES
    CONFIGURE_DEPENDS  # 重要：当添加新文件时自动重新生成 build system
    "*.cpp"
)
set(BIND_QABSTRACTBUTTON_SOURCES ${BIND_QABSTRACTBUTTON_SOURCES} PARENT_SCOPE)